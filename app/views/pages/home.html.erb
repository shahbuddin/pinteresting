<div class="jumbotron center">
	<hgroup>
		<h1>Purge off the weight for 2014</h1>
		<h2>Protect versus Perform, which side are you on?</h2>
	</hgroup>
	
	<div class="mod-signup">
		<% if user_signed_in? %>
			
  			<div>Signed in as... <%= current_user.email %></div>
			<div>Show Dashboard Here</div>

			<% @users = User.all %>
			<% @users.each do |user| %>
  				<h2><%= user.name %></h2>
  				<h3><%= current_user.weights.last.weight %></h3>
			<% end %>


		<% else %>
			<%= link_to "Sign up for free", new_user_registration_path, class: "btn btn-primary btn-lg" %>
		<% end %>
	</div>
</div>